if(BUILD_GEOMETRY)
    add_subdirectory(geometry)
endif()
if(BUILD_UI)
    add_subdirectory(ui)
endif()
if(BUILD_AI)
    add_subdirectory(ai)
endif()
if(BUILD_NLP)
    add_subdirectory(nlp)
endif()
if(BUILD_SECURITY)
    add_subdirectory(security)
endif()